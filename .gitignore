# Claude Code project instructions (local dev only)
CLAUDE.md
ELEMENTOR_WIDGET_GUIDE.md

# Composer dependencies — vendor/ excluded; lock file committed for reproducible CI builds
vendor/

# Node.js dependencies — node_modules/ excluded; lock file committed for reproducible CI builds
node_modules/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Build artefacts
*.zip
